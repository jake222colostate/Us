import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'node:path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@us/ui': path.resolve(__dirname, '../../packages/ui/src'),
      '@': path.resolve(__dirname, 'src'),
      // Force web shims:
      '@react-native-community/slider': path.resolve(__dirname, 'src/shims/slider.tsx'),
      'expo-linear-gradient': path.resolve(__dirname, 'src/shims/linear-gradient-web.tsx'),
      'react-native': path.resolve(__dirname, 'src/_shims/react-native-empty.tsx'),
    },
  },
  server: {
    host: true,
    port: 8000,
    strictPort: true,
    hmr: { overlay: true },
  },
})
