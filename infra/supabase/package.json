{
  "name": "supabase-infra",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "migrate:dev": "supabase db push --env-file .env.dev",
    "seed": "supabase db seed --env-file .env.dev"
  },
  "devDependencies": {}
}
